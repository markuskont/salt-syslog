#Generated by Salt

source s_src {
  {% if grains.init == 'systemd' %}
  systemd-journal(prefix(".SDATA.journald."));
  {% else %}
  system();
  internal();
  {% endif %}
};

destination d_local_self {
  file("/var/log/{{ grains.fqdn }}/$YEAR.$MONTH.$DAY");
};

log {
  source(s_src);
  destination(d_local_self);
};
